<h1>
    <%= @board.title %>
</h1>


<%= link_to "新增文章", new_board_post_path(@board) %>

    <ul>
        <% @posts.each do |post| %>
            <li>
                <%= post.title %>
                    <li>
                        <% end %>
    </ul>